/**
 * To Loop through all elements and Get a new modified array
 */

let fruits = ['apple', 'mango', 'cherry', 'grapes'];
let upperCaseName = fruits.map(fruit => fruit.toUpperCase());
console.log(upperCaseName);